# Skills Moved

Workflow skills now live in the repository root:

**[`skills/`](../../skills/README.md)**

Use `.mdc` skills from that directory. Cursor discovers them via `.cursor/rules/skills-discovery.mdc`.
