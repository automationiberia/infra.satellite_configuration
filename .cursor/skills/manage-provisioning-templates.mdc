---
description: Manage provisioning templates and OS default template associations in Satellite.
alwaysApply: false
---

# Manage Provisioning Templates

Use this for `satellite_provisioning_templates`.

## Steps
1. Define templates in `satellite_provisioning_templates`.
2. Apply template objects with `redhat.satellite.provisioning_template`.
3. Apply default OS mappings with `redhat.satellite.os_default_template` where needed.
4. Keep template content and metadata under source control.

## Checklist
- Uses FQCN `redhat.satellite.provisioning_template`.
- Uses FQCN `redhat.satellite.os_default_template` for defaults.
- Preserves deterministic template names.
