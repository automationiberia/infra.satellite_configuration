---
bugfixes:
  - dispatch - avoid recursive Jinja self-reference when passing
    satellite_operatingsystems into redhat.satellite.operatingsystems
    (Galaxy Importer ansible-lint jinja[invalid]).
minor_changes:
  - Ship .ansible-lint in the collection artifact so Galaxy Importer respects
    collection skip_list (for example internal-error without redhat.satellite).
  - Rename content_views_purge_count default to
    satellite_configuration_dispatch_content_views_purge_count (compat alias kept).
  - Run galaxy-importer in pre-commit (and extend local ansible-doc checks to
    filters) so CRC/Galaxy import findings fail before a release is published.
...
